export type navigationContainerProps = {
  navigation: Object
};

export type navigationContainerState = {
  region: Object,
};

export type navigationProps = {
  openNavigation: Function,
  lang: Object
};
